{
  "driver": "grok",
  "notes": "No static wire capture: Grok speaks ACP over a real child process. This 'fixture' is recorded live each run by spawning apps/server/scripts/acp-mock-agent.ts (undriven by any T3_ACP_EMIT_* env var) through the real makeGrokAdapter — the same mock GrokAdapter.test.ts's own base scenario uses. Fixed scenario: startSession(model:'grok-mock-alt') -> sendTurn('hello grok') -> collect until turn.completed plus a short quiescence window. See apps/server/src/spi/replay/acpReplay.ts (recordGrokBaseline).",
  "synthetic": true
}
